# Sistema de Sincronización Inteligente
# Instalar con: pip install -r requirements.txt

# Conexión a bases de datos
mysql-connector-python==8.2.0
psycopg2-binary==2.9.7
pymysql>=1.0.0              # MySQL adapter alternativo (pure Python)

# Variables de entorno
python-dotenv==1.0.0

# HTTP client para API de tipo de cambio
requests>=2.31.0

# Servicios de Windows (necesario para sync_service.py)
pywin32==306

# Compilación a ejecutable
pyinstaller==6.1.0

# Otras dependencias
pillow==10.1.0
bcrypt==4.3.0

# System Tray y notificaciones
pystray>=0.19.5
plyer>=2.1.0               # Notificaciones multiplataforma (más estable que win10toast)
win10toast>=0.2            # Backup para notificaciones Windows